name: Cross-Compile Car Controls to aarch64

on:
  push:
    branches:
      - 43-github-actions-compilation
  pull_request:
    branches:
      - 43-github-actions-compilation

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3
      
      - name: Copy Files to Docker
        run: |
          cp -r /home/michel/actions-runner/_Work/HotWheels-Cluster/car_controls /home/michel/docker/

      - name: Build for AArch64
        run: |
          cd /home/michel/docker
          docker exec sad_curran /bin/bash -c "cd car_controls && make"

      - name: Upload AArch64 Executable
        uses: actions/upload-artifact@v3
        with:
          name: aarch64-executable
          path: /home/michel/docker/car_controls/car_controls